# Curated winning override scripts

Each `<game>.cmd` here is a hand-authored command script that drives its game to a
genuine ending — or, for the one game that provably has none (WAKE), to the
furthest state the game can reach — for a game whose *raw* walkthrough does not
reach the end as-is —
or (the seven formerly hints-only games) has no linear walkthrough at all, only
Q&A/prose hints from which the script was derived against the game source — or
(The Shack, The Tree, The Zen Garden, Iron John, La Aventura de Parodi, Warriors,
Bear's Epic Quest, and every later addition)
has **no published walkthrough of any kind**, the script being derived entirely
from the game source (its `corpus.tsv` walkthrough column is `-`).
When `overrides/<game>.cmd` exists, `run_corpus.sh` uses it verbatim (including any
title-screen preamble baked into line 1) instead of running `extract_walkthrough.py`
on the walkthrough. Every override starts with a `#`-comment header documenting
exactly how it deviates from the raw walkthrough — the goal is "faithful to the
walkthrough's *intent*, corrected only where the walkthrough or the extractor could
not reach the win."

Why each game needs an override (all verified against the game source, seed 1234):

| game | state | reason |
|---|---|---|
| Dracula | Finished | walkthrough is for the *original 1986 CRL* game, not this 2014 remake — same plot/map, different parser. Heavy adaptation: systematic `look X`→`examine X` / `touch X`→`feel X`; per-scene fixes (reception `examine desk` reveals the bell; coach `no`+`jonathan harker`; `close eyes` for the cross; `feel mouth`; `move rail`/`move wardrobe`; `drop cross` in the box + `south`×3; store `pay storekeeper`; post-office `examine s` mail-slot; `sleep` overnight; rail `stratford`/`hawkins`; Renfield `drop net`; finale `chop tree`, spade-handle+carving-knife stake, `focus monocle` to burn the crypt door, `drop corn`, garlic to repel Dracula, tunnels→mausoleum). Header documents every deviation. |
| Dream Pieces | Finished | walkthrough's last step `unlock door` is rejected ("Try unlocking the lock."); the real verb is `unlock lock`. `new game` preamble baked in. |
| Jacqueline, Jungle Queen! | Finished | two mutually-exclusive endings (running both corrupts state); keep only Ending 2 "Radio for Help". Also fixes the `pirahna`→`piranha` typo that silently broke the swim ability, and one nav step vs the release map. |
| Guttersnipe- Carnival of Regrets | Finished | two walkthrough nav errors vs the actual room map (`w`→`e` at the Strong Man Tent; insert `in` before a spook-house descent). One early wrong turn cascaded through the whole tail. |
| Escape From the Mechanical Bathhouse | Finished | RNG-/timing-specific solve: the mechanical man wanders on a seeded turnscript (`z` exactly 7× to catch him) and the 5 puzzle symbols are seed-1234 draws; insert the opposite face each time. The 50s limit is real wall-clock, which the headless oracle never advances (see main README, DrainTimers), so turns are unlimited. |
| The Mouse Who Woke Up For Christmas | Finished | needs the oracle's `DrainTimers` (a `<dark>` room's reveal is on `SetTimeout(2)`; without firing timers the room stays black and the game is unwinnable). Script drops the walkthrough's stray `Continue` lines and a `put bowl in moonbeam` the walkthrough itself flags as wrong. |
| The Deer Trail | Finished | author's own walkthrough (named after the game's working title "The Deer Blind") is a bare command list in a navigation shorthand the parser rejects: every "Direction to \<room\>" line -> the bare compass direction; "knock arrow" -> `nock arrow` (the game's verb — "knock" hits the generic knock verb and leaves the bow unloaded); "westto…"/"north bedroom" missing-space/-"to" typos; "use stairs/steps to \<room\>" / "enter laundry shoot to cellar" -> the object command without the destination. Ending never calls `finish` (invites RESTART/UNDO), so the script ends with the game's own `quit` command (`^quit$` → `finish`), as with Serpent's Eye. |
| The Bony King of Nowhere | Finished (ERR=31) | fixes a `.s` extraction/typo artifact and adds `z` waits for a `SetTimeout(5)` guard change. Reaches the genuine ending + credits; the 31 errors are a QuestViva display-recursion bug on the mandatory `attack general`→prison POV swap and do NOT trip the real breaker (`_scriptErrorsFatal` stays false — see main README). |
| I Contain Multitudes | Finished | drives cleanly (0 errors) through the Heart reveal to the full "Ending." decision-outcome credits. **Was the corpus's one harness-unreachable win until 2026-07-20**, and the old diagnosis (a nested `wait{…wait{…SetTimeout(7)…}}` continuation the harness would not pump) was wrong: the real blocker was a *synchronous* `play sound`, which parks the rest of the script on the wait slot with nothing to release it headless. Fixed in `HeadlessPlayer.PlaySoundAsync` — see the main README. The game's own author warns its time-based events break Quest's *own* walkthrough runner. |
| Whitefield Academy of Witchcraft | Finished (errors=34) | `#!errorlimit=200`: the *mandatory* Grislewood snare double-teleports the player into "Inescapable Cage" — a room reachable only by that teleport that never gets map coordinates — whose enter-script throws ~19 `DictionaryItem(coordinates,"x")` errors in one transition, tripping QuestViva's 20-error breaker (absent in legacy Quest, where an enter-script error is non-fatal). The storm is finite (34 errors over the whole 440-step game), so lifting the limit lets David Welbourn's full walkthrough run to the genuine THE END — all students saved. Same pattern as The Acreage. |
| Night House | Finished | hints-only Q&A walkthrough linearised from the hints + game source. "Light world" front-door ending (cartridge to the ghost boy sets `pit.ghostgone`). Notable verb renders: `use scissors on strapping tape`, `push dummy` to plant the decoy, `put baby monitor in pit`; mandatory stage-advancing examines (`look at dark figure`, `look at sewing machine`). |
| Poppet | Finished | Welbourn walkthrough-and-map (the author's own walkthrough is Q&A hints); two fixes: `Piroutte`→`Pirouette` typo, and a `2` answering the claw disambiguation menu after `use claw on shoelace`. Good ending ("Good night, Poppet." — the teapot/bad-ending path is never touched). |
| What Once Was | Finished | hints-only author walkthrough; entire 183-step script derived from hints + source. Ending 1 of 2 (Trust Alan → Redeemed Dean in the pub). Fate menu is Core's numbered *text* menu (bare `2`); `give marker to phil` must precede the cartridge to dodge a disambiguation; keypad/Greggs/quiet-room get-inputs answered as plain lines; `step into temporal field` (the `use` path is an author bug); pub ending = `SetTurnTimeout(7)` satisfied with 6 waits. |
| Hawk the Hunter | Finished | prose hints only; hardest derivation in the corpus. Sacrifice ending (`save rap`). Stats str 6 (Mindsword pull requires str>3); acolytes killed by full name (disambiguation); the River Shale is impassable, so the Weir-maze N,E,W,S teleport is used 3× (trip 1 = scripted spy vignette, trip 2 spends Great Hall's `beforefirstenter` re-arm, trip 3 sticks); font `wash me` blessing deflects the chapel blood-spray; exactly 6 post-wash Great Hall turns arm the Voltan dilemma; padded `save rap` lines make the one-turn window unmissable. |
| Eight characters, a number, and a happy ending | Finished | the guide's own linear `FULL WALKTHRU: "Good" ending` tail, adapted: `Look X`→`examine X` (only look-at/x/examine parse); dropped duplicate examine/get lines; `computer iastrai` lowercased (the command LCase()s its arg); password `28831010` answers the self-destruct get-input. DROPPED `turn on game board`: the three board objects mutually `SwitchOn` each other and QuestViva's changed-script dispatch recurses to depth 200 and trips the breaker (a real QuestViva incompatibility — and a `changedswitchedon` test case for the native engine); `press play` never checks the board state, so the story beat still fires. |
| Quest for the Serpent's Eye | Finished | author's prose "Full Solution" rendered into parser vocabulary. Good ending (`put eye on altar`). Fixes: `close manual` before the floppy; ravine tinder is the *hint book*; `lower rope down cliff` (bare `lower rope` opens a menu without the cliff); cryptogram answer `what`; `play flute` must be the turn right after `take eye` (strict `SetTurnTimeout` fight); throw the banana *peel*. The good ending parks in the Ending Room without calling `finish`, so the script ends with the game's own `quit` command (`^quit$` → `finish`) after the full ending text. **v1.1.3 verified compatible** (2026-07-18): the newer release in the games dir (`Quest for the Serpent's Eye v1.1.3.quest`) replays this exact frozen script to THE END with errors=0 — same path, 16 prose-polish diff lines (dias→pedestal etc.). The corpus row stays frozen on v1.1.1 (no golden churn). |
| The Brutal Murder of Jenny Lee | Finished | official PDF walkthrough (extracted to text — the only PDF-only walkthrough in the corpus), near-literal commands. Ending 1 "escape" (deterministic `finish`; Ending 2 would idle 15 turns through a `SetTurnTimeout(15)` monologue). "Go in any direction N times" rendered as explicit moves; keypad codes (`1953`, `0903`, `4489`, `2471`) answer get-input prompts. |
| The Shack | Finished (ERR=2) | no published walkthrough exists at all — the 198-step script was derived from the game source (Void Cube ending: three key parts on the Shoreline statue, jack the underground steel door, drop the cube in the White Room). `take brass key` (the object's alias is "tiny brass key"; its source name "padlock key" is unmatchable). The 2 errors are the game's own `open drawer` recursion (onopen re-sets `isopen`, re-firing the changed chain to the 200-depth cap, twice) — mandatory to reveal the metal box, output correct, breaker untouched. Recurring `Overall` flavour timer stays dormant headless. |
| The Tree | Finished | no published walkthrough exists at all — the 94-step script was derived from the game source (win = `sit on control chair` in the Aries while carrying the dog Cassie; passwords `say cassie` from Crusher's tag / `say 4976` from the locker picture; the Updater headphones education uses nested SetTimeouts, drained by the oracle). Two scope beats matter: `x dashboard` before `put spike in control port` and `x capsules` before `say 4976` (hidechildren). Notable engine find: `insert_spike`'s **synchronous** `play sound ("click.mp3", true, false)` parks the rest of the script (LockExit + Crusher reveal) on the wait slot until the next `wait{}` (the tube holcast) pumps it — the native engine grew parked-continuation support to match (aslx-runtime TurnSuspended frames). |
| Iron John | Finished | Greek-language game (Σιδηρόγιαννος, 2018); no published walkthrough exists — the 124-step script was derived from the game source. Genuine ending (`kef6` → ΤΕΛΟΣ) at MAX score 35/35 (all 20 `scoreup` events). Timing gates: reach the throne room within 7 turns of the start; follow the dog's scent trail so `d7limni` is entered at exactly `dog.startsmell+5`; give the σημείωμα immediately (the gardener evicts you after 2 turns); give the princess the *livadi wildflowers* (9-turn wilt timer) — the garden flowers are the unscored decoy. Notable engine find: the native engine's ASCII-only LCase/UCase/CapFirst broke Greek alias matching (`βασιλιά` vs `Βασιλιά`) and `CapFirst("το")` — fixed with .NET-style Unicode simple case mapping in aslx-runtime-builtins.inc. |
| La Aventura de Parodi | Finished | Spanish-language joke game (Lucas Cavanna Quero, 2014); no published walkthrough exists — the 43-step script was derived from the game source. Win = `matar panchi` on Panchi el Magnífico in "el final" (calls `finish` unconditionally); script enriched to hit every authored response. Finds: the game has NO `<object name="player">` at all (engine implicit default player); Spanish ParserIgnorePrefixes lacks "a" (`matar panchi`, not `matar a panchi`); verb pattern alternatives match in order, so `sientate lucas` not `sientate en lucas`; street-Panchi is killed twice (MakeObjectInvisible) before the boss so the bare "Panchi" alias is never shadowed. |
| Warriors | Finished | Warrior Cats CYOA-style fan demo (2016); no published walkthrough exists — the 25-step script was derived from the game source. All 13 `finish` sites print "END OF DEMO."; the script takes the richest arc: character creation → Chapter One "Night play" (fresh-kill snack + prey-play, sneak out the dirtplace hole, forest exploration, back to bed → Complete! + status/code screen) → Chapter Two "Play time" keep-away → the flying-leap toss ending. Two menu systems interleave (Core `ShowMenu`/`Ask` = numbered text menus answered with bare numbers; `show menu` = engine menus answered with `menu:KEY`). Avoids the one-color pelt path (derefs unset `player.colorO` in a wait block — author bug); seed-1234 RNG lands sister Fernkit and the "bats you away" wake-up (get-input answered "No"). |
| Bear's Epic Quest | Finished | joke game (Hospes, 2011, ASL 500 — oldest ASL version in the corpus); no published walkthrough exists — the 19-step script was derived from the game source. The game's only ending: `sleep on stone` (custom "Sleep on" verb) on the Comfy Stone calls `finish`. Route gates: `x bear` (player look script) unlocks the Great Outdoors exits; `shit on leaves` sets the flag gating `eat carcass` (after `kill deer`); eating the carcass creates the Dark Cave exit. Quirks: the bushes exit's alias overrides its `in` direction, so only `go and enter some bushes` works; `piss on god` spawns the dead astronaut (optional flavour, included). No score/RNG/timers; ASL 500 doubles articles ("a A juicy deer") — the game's own bug. |
| Sueña un pequeño sueño | **Running** (genuine end) | Spanish gamebook (Juan Cristóbal Aedo Olivares, 2014, ASL 540, `_editorstyle=gamebook`); no published walkthrough exists — the 7-step route was derived from the game source. Commands are PAGE NAMES (gamebook `HandleCommand` does `GetObject(command)`), not verbs — accented names must match exactly (`conversación`, `En mi habitación`). True ending "En mi habitación" (wake from a 3-year coma; María died in the crash), touring the stay-home branch first; the surrender-to-Death page "Muerte" is the decoy ending. The source contains ZERO `finish` calls, so parking on the option-less terminal page at `state=Running` IS the game's complete ending. |
| El asesino durmiente (Micky) | **Running** (genuine end) | Spanish horror gamebook (CandyVonBitter, 2016, ASL 550, `_editorstyle=gamebook`); no published walkthrough exists — the 7-step route was derived from the game source. Commands are page object names, incl. accented multi-word names (`Aceptar bebidas`, `Alguien en algún lado está anotando algo`). Richer ending: accept you are the killer → "Los sueños se hacen realidad." → the epilogue page delivering the twist (the neighbour boy is the real killer; you are "subject 21"); the deny-branch (Page9) is shorter with no epilogue. Conditional page text via `{if PageN.visited:}`. ZERO `finish` calls in source, so `state=Running` on the option-less epilogue page IS the complete ending. |
| Medievalist's Quest | **Running** (genuine end) | gamebook (Cein Chavez, 2017, ASL 550, `_editorstyle=gamebook`; the .quest is 122MB of media around a 74KB game.aslx); no published walkthrough exists — the 49-step route was derived from the game source. Commands are page NAMES (options-dictionary keys), not the displayed link text. Ends on the authored terminal page `Part 1 end` ("End of part 1" — the game is explicitly 1/3 of the story); zero `finish()` in source, so `state=Running` there is the genuine complete ending. Author bug: in the soleclops fight, `Bout1`'s Fight/Item options loop back to the FIRST battle's pages, making the `Strength` page unreachable — the working kill route is Spunk Spell → Punch → Spunking it up. Script also tours the three key-hunt searches and battle hesitation/blocked-run flavour (all deterministic loops; no RNG anywhere on the path). |
| Balaclava | **Running** (genuine end) | bilingual surreal gamebook (nahuel denegri, 2015, ASL 550, `_editorstyle=gamebook`); no published walkthrough exists — the 26-step route was derived from the game source. Commands are page names (`Page2`…`Page27`). Page1 forks ENGLISH/ESPAÑOL arcs of the same story; the script takes the English arc and tours all three apartment activities (TV → balcony → the mandatory drawer search) before the balaclava reveal. Ends on the option-less credits page Page27; zero `finish()` in source, so `state=Running` there is the complete ending. No score/RNG/timers. |
| cuttings | **Running** (genuine end) | surreal gamebook (nahuel denegri, 2014, ASL 550, `_editorstyle=gamebook`); no published walkthrough exists — derived from the game source. PURELY LINEAR: a single 46-step chain Page2→…→Page47 (every page has exactly one link; no branches). Ends on the option-less credits page Page47; zero `finish()` in source, so `state=Running` there is the complete ending. Pages carry `play sound`/`stop sound` flavour scripts — harmless headless. No score/RNG/timers. |
| Everyman | Finished | comedy adventure (Simon Deimel, 2013, ASL 540); no published walkthrough exists — the 69-step script was derived from the game source. Win = on stage, `switch on microphone` then `tell joke` (the joke object's tell verb calls the game's only `finish`). Gates: `x leaflet` (firsttime) unlocks the front door AND hands over the application form; leaving the flat is one-way (the front-door script locks the return exit) so pen/hammer/towel/milk/knife/frying-pan/pill must be collected first; `use sleeping pill on milk` mixes the guard's sleeping potion (blocked at the temple itself); pew stain wiped with the towel reveals code 69105 for the electronic device (get-input answer); frying pan KOs the goblin guarding the holy book of jokes ("joker" flag); hammer + knife open the fence/costume-shop path to the clown suit, wearable only in the wardrobe; stage exit requires suit+joker. errors=0, no RNG/timers. |
| Exit the Room | Finished | single-room escape game (jerkdavi, 2021, ASL 580); no published walkthrough exists — the 22-step script was derived from the game source. Built on a hand-rolled two-letter flag machine; win = `knock on door` once knck="av", set only after BOTH `read matthew 7:7-8` (Bible in hand + the rd chain from `x note` ["7:7-8" scribble] and `x icon` [Matthew]) and the copykey failing in the lock. The copykey drops out of `compare doorkey to keys` ("yes" answers the are-you-sure get-input; the 20-key counting wait{} chain auto-continues). Faith moral: keys never work, knocking does. Scope gates: doorkey taken from the lock after `x door`; `x bed` before the pillow; box appears after `move tunic`, opens only OUT of the closet. 100-turn insanity limit (script = 22 turns); recurring Clang timers dormant headless. |
| The Gift of the Magi | Finished | O. Henry story adaptation (2014, ASL 550, complete Project Gutenberg text); no published walkthrough exists — the 59-step script was derived from the game source. Guided-story engine: 50 numbered text beats driven by a `game."Next text"` counter, each unlocked by ONE specific interaction (the status bar's "Next interaction" is the hint; wrong-time interactions print "Nothing to see at this moment"). Custom verbs: `flop on`/`look out`/`dialog`/`pay`/`take out`/`light`/`fix hair`/`sit at`/`hug`/`reach in pocket`/`unwrap`; movement = `go to <room>` over nameless exits (compass hidden). Curling irons hide in a closed cupboard (`open cupboard`). Ending: beat #49 (`dialog jim` on the couch) prints the #50 magi moral and calls finish after an auto-continued press-any-key wait. No score/RNG/timers. |
| Welcome to the Paris Hotel! | Finished | custom-fiction component (CentaurOfAttn, 2017, ASL 550); no published walkthrough exists — the 51-step script was derived from the game source. Ending: `x paper` (the slip in Room 1988's Gideon Bible) decodes Marilyn's letter (TextFX_Unscramble behind a drained SetTimeout) and calls finish. Chain: Journey slot's gold lever (firsttime jackpot, no RNG) → voucher → redemption hub → $2 → gum; crepe-stand "A" tile fixes the mural's ROT_TING→ROTATING (`use tile on space`) opening the secret room; chewed gum + wrapper + button-on-a-string = sticky button that fishes Marilyn's ID from the grate; attendant answered via two get-inputs ("Marilyn Copeland", "yes") hands the spare key past the elevator guard; `push 19` in the elevator. Parser gotcha: `use gum on button` — the full alias "button on a string" makes the useon regex split at the LAST " on ". `take paper` skipped: it hits the game's own "[error] No verb response defined" (x works without taking). |
| Texture | Finished | adult horror story (Ralf Thissen, 2015, ASL 550); no published walkthrough exists — the 48-step script was derived from the game source. Ending: `use emblem on small hole` → "forest ending" (arrested for the wife's murder) → finish. Chain: door-mat key → front door; bloody kitchen knife on the toilet seat; Anne Frank book-lever opens the secret room; knife held to the attic mirror crosses into the mirror world (knife turns clean); power supply (kitchen cabinet) revives the laptop whose police article arms the painting; knife-on-mirror returns; `x painting` flashes back to the packing wife; `kill wife` (mandatory scene) yields the emblem. The optional explicit scene is not taken. No score/RNG/timers. |
| Quest for Loot and Something Else | Finished | birthday giveaway game (Mugi4ok, 2016, ASL 550; rooms print Steam links as "loot"); no published walkthrough exists — the 83-step script was derived from the game source. Ending: the "strange place" asks for gratitude via two get-inputs ("thanks", "yes") → finish; it unlocks after entering the dark `future` room (eastComplete) + `use portal` at the end of the west walk (westComplete; the portal also re-sets eastComplete — an author shortcut the script does not rely on, the east is played properly). East chain: noir red scarf hides the ridiculously huge key → freaking giant keyhole; time-room numbers note + naval device → witcher world round trip; spaceship → space → wasteland sheriff star → Wild-West stranger swaps it for the grey-box medal → battlefield squad; warzone bike keys; Neo Tokyo ship part repairs the ship; `look south` in noir eavesdrops the address note that opens the Tokyo inn. The starting room is DARK — `switch on switch` first. No RNG/timers. |
| Attack On Frightside | Finished | Halloween comedy (Farley Sweet, 2015, ASL 550); no published walkthrough exists — the 48-step script was derived from the game source. Ending: `hug jimmy` in the Throne Room — the redemption ending ("the power a hug can have on a hurt soul", FIN) — over the alternative `fight jimmy`. Chain: `kick cell door`; kitchen Piece of Candy pays the tavern bouncer (`pay bouncer with piece of candy` — the verb takes the "with" separator); the bartender's free first round Jar of Milk revives Bug-a-boo at the Tower Gate (he breaks the gate and eats Chocolate Magma); `push fat jimmie` out the window; `mercy aigee peaitch` (kill/duel/mercy all clear the layer — mercy fits the ending). errors=0, no RNG/timers. |
| Its election time in Pakistan- Go rich boy, go! | **Running** (genuine end) | satirical election-day gamebook (Jay Haque, 2013, ASL 540); no published walkthrough exists — the 15-step route was derived from the game source. Commands are page names. ZERO `finish()` and NO option-less reachable page (every ending only offers "Play the game from the start!"), so the script parks on the ending page without restarting. Score ending: take crush Aaliya to vote, survive the drive to Murdabad (+500 wide turn), thread the rat maze, vote Imran Khan, tell Aaliya truthfully → "You SCORE +20,000 for getting the girl!" (Page119). Dead pages 9/11/12/59/63/69/134 are unreferenced in the release. No RNG/timers. |
| Nearco II (Remake) - La perdición de la Ninfa - Vr Offline | Finished | Spanish adventure (Jhames, 2011, ASL 500); no published walkthrough exists — the 101-step script was derived from the game source. The full ending prints (Playa: "FELICIDADES HAS TERMINADO NEARCO 2" + credits) and the final `finish` now runs: it sits behind a synchronous `play sound ("cantoninfa.mp3", true, false)` with no later wait to pump it, which used to park it forever (this row was `Running` until 2026-07-20 — see the main README's sync-sound note; HMS Victory is the same shape and the native replay host got the matching hook). Chain: `di ninfa si` opens the star labyrinth; `nerea 356` in Cancer (Alexander's natal house) drops the pandigital pergamino, `di zeus serpiente` the anti-snake spray; `di viejo 1036957284` (the magic square's missing number) opens the cemetery; `rocia serpiente con antiserpientes`; the spherical roca pushed twice smashes the tomb (BEFORE any cemetery visit); `x tumba` parks its skull-reveal tail on a sync sound — pumped by the comic cíclope's WaitForKeyPress on the way back through Camino Noroeste; `x cráneo` then `di ninfa teofet` in Aries opens the beach. Two harness finds: the game ships a malformed DefaultPull dynamictemplate (`"No puedes tirar de eso"."`) that legacy Quest tolerated — patch_questviva.py §3 now registers dynamictemplates lazily (load-fatal in stock QuestViva); and the native pre-540 print path learned to skip trailing non-br tags when deciding the qvh newline (PrintCentered ends `<br/></align>`). |
| Caught! | Finished | prison escape (T H Duncan, 2016, ASL 550, health+score, "three ways to win"); no published walkthrough exists — the 44-step script was derived from the game source. Takes the WINDOW escape ("You won! Congratulations!"). Chain: `search straw` → key; flat stone in Cell 2's lidded bucket + `use torch on flat stone` (+6 clue); Midden Pit knife; the kitchen is only survivable in the GUARD uniform (trousers chest 2 + jacket chest 4 — exactly the two chest-opens the creaking allows; boots are a health trap): undisguised entry arms the cook's call-the-guard SetTimeouts, which the oracle drain fires instantly = caught; `cut rope` KOs the cook, uniform swapped for the cook's hat+apron; tray to the Dining Hall guards buys the crossing; Lobby rat scooped into the chef's hat; `use rat on master` on turn 1 beats his 4-turn kill timer; letter opener cuts the curtain rope (length 2) and `open window` slides down. Inventory capped at 5 (flat stone dropped for the tray). No RNG on the path. |
| The Last Hero | **Running** (best-effort — game unwinnable) | three-act comedy quest (Wayde Bairstow, 2014, ASL 550); no published walkthrough exists — the 96-step script was derived from the game source. THE SHIPPED GAME IS UNWINNABLE: every MoveObject into the challenge rooms misspells the room name ("The Warriors/Mages Challange" — no such object), so Duch never spawns, the Bronze key/Titans hammer, Glowing writing and Book of spells never appear, and Gimply's dragon elevator strictly requires all three challenge rewards. The script drives every reachable beat cleanly — window/bee/key, cheese-lured rat, Street shark extract revival, gear crafting, kitty-cat guard kill, King's quest, branch→beaver→plank→boulder marauder crush, Talking-door riddles (dave/snail/man/e get-inputs), the Rogues Challenge win (vase distraction, torch doused before the golden chest) — and parks at the Room of Heros dead end wearing the Rogues cloak. errors=2 = the game's own misspelled-room bugs (engine-parity cases: the oracle reports the identifier ___SPACE___-encoded, which the native engine now mirrors). The Warriors Challenge is never entered — its door locks behind you with an unreachable unlock (permanent soft-lock). All kill timers recurring (dormant headless); no RNG. |
| First Times | **Running** (genuine end) | psychological horror (Hero Robb, ~2012, ASL 520); no published walkthrough exists — the 223-step script was derived from the game source. The game NEVER calls finish: four endings ((redemption\|ignorance) × (resentment\|acceptance)) park in the "end" room — this script reaches the BEST one, **Apocatastasis** (redemption + acceptance). The game gates progression on AUTHORED one-shot real-time timers (title crawl, 69s darkness chase, 90s booth suicide seance, faceless-family chase, choice fade) — this game is why the harness grew the `tick:N` step (advance the clock by exactly the authored interval, deterministically, in both qvh and aslx_replay). Chain: scalpel/sheet→tumor→web cluster→alabaster egg→drawer-666 hole; janitor riddle (`say iron`) → can opener; play money→vending can→earrings; needle bear→needles+safe bear; kiss key→heart room altar (flowers+earrings+bear)→red heart→darkness chase→heart of hate→child doll wrecks the playroom; notebook→ticket; scalpel frees the boy = REDEMPTION + toy tower→mural stair→plastic tube; ticket in booth slot→hands of neglect→teen doll→ritual room plunger; syringe+venom injection→choice (nw = ACCEPTANCE)→eyes of suicide→mature doll→stone knife→fleshy Descent door→memory windows→Depth mirror. errors=0, deterministic. |
| To Kindle A Light | Finished | mystery novella (J. Vasilevic, 2014, ASL 550); no published walkthrough exists — the 61-step winning script was derived from the game source. Melanie sorts her dead uncle's New Zealand farmhouse; ending = after reading the captive's notebook in the underground room (the "knows" revelation), return uncle Mark's photo to the station shack window (`put paper in window`) → "THE END." + finish. Chain: chopped log smashes the garage padlock; the reeking front corridor bounces you out until the garage-rear cleaning products are fetched (rear is DARK — torch on; never open its casing, the batteries come out for good); Aga tray = the severed-thumbs story beat; DEN small key → office, where correcting the flip calendar drops the yale key → dining room; `push bureau` reveals the wall safe, `set dial to 3367` opens the crawlspace to the underground room. errors=0, no RNG; crawlspace SetTimeouts drain harmlessly. |
| Tombs and Mummies | Finished | tomb-escape puzzler (Matthew Warner, 2020, ASL 580, health + torch-wick economy); no published walkthrough exists — the 22-step winning script was derived from the game source. Ending: climb the propped ladder out of the entry-room hatch ("You escaped!"). Chain: ignite the sconce torch (wick burns 5%/turn — the script fits in one match); Ark of Magic papyrus teaches `open sesame` (hidden burial door), the hieroglyphs teach `appum`; the Flail of Anubis locks the sarcophagus so the appum-animated mummy stays imprisoned; appum lightens the Horus-shrine ladder; prop and climb. All combat/lid-beating timers are real-time (dormant headless). Traps avoided: the Amulet of Disorientation (randomises exits) and the Snake Statue (body-swaps you into the mummy). No RNG on the path. |
| The Acreage | Finished | sci-fi narrative (D.C. Tozier, 2025, ASL 580, ShowMenu conversation topics); no published walkthrough exists — the 93-step winning script was derived from the game source. COMPLETE GOOD-PATH WIN: Desmond boards as a loyal follower (never drugged, never signed away), the nuns collect Isaac on Surst, you are paid — then the game's own `quit` (→ finish), as the ending never calls finish itself. Requires the `#!errorlimit=200` directive: every Port entry re-runs the room description into the 200-depth recursion cap (~20–40 logged errors per visit, 58 total — the game's own bug, hidden interactively by ClearScreen; legacy Quest had no breaker, so raising the limit is the faithful reading). Chain: shattered syringe → essage; Repair Kit → robot cat → Julia referral; food token → Shanti → drink token + Colette's trust → sleeping pill emptied ON THE FLOOR (defying Isaac); the honest beer keeps Desmond loyal; the secretary's debt-scam locks the ship, Isaac demands blood — pistol shot (it shatters), office terminal lifts the flight restrictions; TAKE OFF → ENGAGE FOLD (auto weapons lockup) → LANDING SEQUENCES → Surst. All 58 errors reproduced byte-identically by the native engine. |
| ARC II | Finished | sci-fi narrative sequel (Daniel Gao, ASL 580, 16.5 MB media / 2400 lines of game content); no published walkthrough exists — the 36-step winning script was derived from the game source. Best ending: `receive message display` in the Command Room (Chloe's messages + the necklace paragraph, requires `speak to chloe` in Part One) → "The End" + `finish`. Part One is fully scripted with firsttime-hesitation ladders (`east` ×2, `press button` ×3); Part Two's earthquake is a turn-based `SetTurnTimeout(8)` burned on flavour examines (no `tick:` needed); the North Greenhouse badge gates every remaining door; the Warden forgive menu is Core's numbered text menu (bare `1` — literal "Yes" silently wedges the pending menu). The winning verb needs its object: bare `receive message` is not understood. Alternates (not taken): the Part One walk-away "THE END", and the unforgiven-Warden variant (he hangs himself; still winnable). errors=0. |
| Xanadu - The World's Only Hope | Finished | comedy desert adventure, part 1 of 3 (Kevin Schaeffer, 2015, ASL 550); no published walkthrough exists — the 225-step winning script was derived from the game source. Ending: the Guard Barracks secret platform fires a 2-turn `SetTurnTimeout` capture/twist scene ("Congratulations! You have completed the first installment…") + `finish`. Authored recurring timers driven explicitly (`tick:30` cave light-sliver, `tick:40` odd machine, `tick:24` jail henchman); sticky-lasso bug hunt, copper wire strung machine→lab→rooftops→jail cell (electrocuted henchman drops the gold key), Velma vault code `answer 5326`, spyglass base code `type 28744`, thicket-maze lever + pod launcher. hidechildren containers (belt/planks/desk/scaffolding/glint) each need a `look at` first; `use dirty key on padlock` leaves the twin "Dirty Key1" in inventory (author bug — script drops it); smelling the White Pod is a maze-restart trap, avoided. errors=0. |
| spondre | **Running** (genuine end — Finished unreachable) | hypertext ResponseLib one-room drama (Jay Nabonne, 2014, ASL 550, v1.1a); no published walkthrough exists — the 78-step winning script was derived from the game source. GOOD ending reached: trusting=100/cooperative=35 vs the trusting>=8 AND cooperative>=2 gate (ignored<4), "Repent" → EndPage_Success lab notes → Day 1/Day 2 pages → `end game` → FINALTEXT credits + GAMEOVER. **Finished is provably unreachable**: the game never calls `finish`/request(Quit) — its inlined HandleCommand routes ALL typed input to ResponseLib topic matching, so Core's `quit` command is dead code and the end page offers only a JS Restart reload; Running-at-credits IS the authored terminal state. Driving: `event:TitleDone;0` first; `event:FinishLeadin;…`/`event:EndPage_FadeDone;0` stand in for JS fade callbacks; bullet menus are game-inlined numbered menus (bare number; any non-number cancels into the silent ignored:+1 trap); Spon initiates conversation only after 4-6 consecutive reflect-class turns. errors=1 = the faithful InitInterface-era null-pov UpdatePlayerUI error (legacy Quest errored identically). Unblocked by the oracle dictionary-`in` fix (patch_questviva.py §5). |
| Xanadu - In the Compound - Revenge | Finished (errors=65) | comedy compound-escape, part 2 of 3 (Kevin Schaeffer, 2016, ASL 550, 586 objects, 25 MB media; not previously in the corpus MANIFEST); no published walkthrough exists — the 802-step winning script was derived from the game source, score 92% (253/275). Genuine good ending: ATV escape through the garage (panel code `D@15IN&33GO$62!`) → desert chase → village hut "Congratulations! You have managed to get through Xanadu, Part II"; the win path never calls `finish`, so the script ends with the game's own `quit`. `#!errorlimit=500` required: every Garage visit's enter/onexit `HelperOpen/CloseObject` re-fires changed-isopen to the 200-depth cap (the same author-bug family as The Shack's drawer) — a FINITE ~65-error storm that would trip the 20-error breaker mid-script. Highlights: fake-death cell escape, Blerk possession with wind-gated vent topology, slingshotted rat poison into the water supply, Mamouf disguise + a burn-turn workaround for a silent null-counter shower turnscript bug, chlorine-gassing the lab, Sun-Tzu `bathory` keypad, all six vehicles disabled. Marble camera-throw is a 50% RNG check buffered with deterministic seed-1234 retries. |
| WAKE | **Running** (game is UNWINNABLE) | post-apocalyptic sci-fi **demo**, "Evolution through Extinction" (Timothy Sibiski / "Asyranok", 2013, ASL 540, v1.05 / Demo 2.2); no published walkthrough exists — the 113-step maximal script was derived from the game source. **The only corpus row that is `Running` because the GAME is broken, not because the harness can't pump it** (contrast spondre, which is Running *at its credits* and is a genuine win). `Activate MARS` does `ChangePOV(RCSR 02)`, but the Reactor Core's `attemptreset` then does `MoveObject (player, ReactorPuzzleRoom)` — moving the **`player` object, not `game.pov`** — so the ten fuel rods land in a room the POV is not in, and `ReactorPuzzleRoom` has no exits; `shift rod 1a` is permanently out of scope (asserted: `game.pov = RCSR 02`, `game.pov.parent = ReactorCoreRoom`, `player.parent = ReactorPuzzleRoom`). Faithful, not an oracle artifact — real Quest 5 resolves scope from `game.pov` identically, and the author's own win branch reads *"That did it! … (Placeholder message. Need bunker door to be unlocked)"*. Solving it would only unlock `MilitaryBunker` (description `msg("")`); `SubwayStation`/`TracksSequence`/`BunkerHUB` are orphaned. The rod puzzle is nonetheless solved on paper in the script header: an empty `<position />` element is boolean TRUE in ASLX (asserted), giving start 1A=T 2A=F 3A=T 4A=F 5A=T / 1B=F 2B=F 3B=T 4B=T 5B=T, and the unique GF(2) solution `shift Rod 2A, 4A, 1B, 2B, 3B`. Route: constellation evolution-order puzzle in the Cold Void, five bone fragments to bridge the live wires, pawn-shop safe `147888`, iron-door keypad `2700245830` (Ripley's dog tags), reactor terminal password `LIFE86Z0J` (never hinted in the demo), magic 8 ball riddle `octopus`. `search for tank emblem` is omitted: it raises "Too few parameters passed to ShowMenu" (game passes 2 args + callback, Core needs 3) — optional flavour, left out to keep errors=0. `RoboticsRoom.audiocouplings` is `visible=false` and nothing ever reveals it, so the survivor recording can never be repaired. errors=0; native `aslx_replay` byte-identical. |
| HMS Victory | Finished | ghost-story treasure hunt aboard Nelson's flagship (Peter Edwards, 2012/2018, ASL 550); no published walkthrough exists — the 78-step winning script was derived from the game source. Win = carry all eight of Nelson's relics into The Great Cabin (eight nested `ListContains(ScopeVisible(), …)` in its `enter`). Exit aliases are aft/forward/port/starboard, which Core's `go` pattern does **not** match bare, so every move is spelled `go <alias>`. Signature puzzle: the Grand Magazine kills you carrying the lantern (powder explosion) *and* kills you arriving dark, so the lantern must be dropped in the light room — which sets its "a" flag — and you must walk back out empty-handed, since re-entering the narrow passage *with* it clears the flag again; it then burns behind the reinforced window and lights the magazine from safety. Also: the wardroom's Marine sentry ejects you unless `Got(hat)` (purser's store, past the rats — the Bosun's rat poison is mandatory for orlop deck aft); four rooms are fatal without the lantern; `open` auto-unlocks (this game wires no unlock verb) using the key under the Captain's carpet and the gold key in the pump well, which must first be drained with four strokes of the bilge pumps; the Fighting Top climb is invisible until `x mast`. Decoy "telescope by the window" / "glass on the table" sit in the Great Cabin and are never referenced there. **This game is why the harness learned to complete synchronous sounds**: its ending is `play sound ("Eight bells.wav", true, false)` immediately before the closing message and `finish`. errors=0. |
| Lost in the Shadows of Time | Finished | fantasy soul-splicing story (K.A. Williams, 2013, ASL 540; self-described difficulty "Hard", cruelty "Nasty"); no published walkthrough exists — the 61-step winning script was derived from the game source. **The ending is a death**: you are a road-accident victim spliced into a Wood Elf, and `attack nezrai` in the Castle of Shadows gets you stabbed and wakes you from the coma in a hospital bed; the win then needs `ask nurse about who`, whose nested waits print the credits and call `finish` (`tell nezrai about world` is the other branch and dead-ends on "It seems your only option"). **Exits resolve by alias, not by inherited direction** — six exits carry a custom alias while still inheriting `northdirection`/etc., and the inherit only fills the map slot, so a bare compass word there is "You can't go there" despite the room banner: `go gate of hell`, `go through the gate of loss to the bog of whispers`, `go fly`, `go secret cave entrance`, `go violet`, `go to secret tunnel`. Strictly-ordered chain: glass bottle → spider venom; `ask troll about whiskey` for the EMPTY flask, which must be collected *before* flying east (the Peddler only sells to someone already holding it, and the Hellish Plains exit is one-way); Lavender wants venom THEN pixie → Hellish Key → Gate of Hell; Spider Web traded to Na'shan → White Flute + Gold Coin; `play flute` unlocks the Hell Bird; flask + coin → whiskey → troll → Slimey Key → Gate of Loss. The Bog of Whispers is a light maze where every other exit is an instant `finish` (turtle, mosquito, quicksand, carnivorous plant, rat) — the path is southwest, southwest, south, southeast, `go violet`, "Violet" being the answer to the sign's "WHAT COLOR ARE MY EYES? --LAVENDER". Heartstring Cave is dark (Adventurer's torch) and its Skull Door wants the next triangular number after 3 6 10 — `tell skull about 15`. A `CONSTANT` turnscript prints "What are you going to do?" every turn: authored noise, not an error. errors=0, no RNG. |
| The Legend of Robin Hood | Finished | historical-fantasy retelling in six acts (Craig Dutton, 2013, ASL 540, v1.4; self-described difficulty "Hard", cruelty "Nasty"); no published walkthrough exists — the game's own `walkthru` command answers *"Available in my IF COMP game file as a WORD document"*, so the 170-step winning script was derived from the game source. **This game is why the engines learned to defer `FinishTurn` across a `wait`** (patch_questviva.py §6): the Sheriff's prize script strips your cloak and then does `wait { … MoveObject(player, cloisters) }`, while the archery field's `ArcheryContestStarts` turnscript — armed at contest entry and never disabled — calls `finish` on any turn you stand there unhooded. Running FinishTurn before the wait callback fired it in a room the callback was about to leave, making the win unreachable; legacy Quest blocks the game thread inside `wait`, so the player is already in the cloisters. Act structure, each ending in a cutscene teleport: shoot the deer to get arrested (Sherwood) → skin and eat the rabbit inside a 10-turn starvation turnscript, Will Scarlet's iron key (cell) → quarterstaff duel (bridge) → the archery contest (Nottingham Fair) → unmask Marian in the belfry (St Mary's) → the castle raid. Longest chain is the fair: iron chain → pliers → frogspawn → bent coin, and wheat → grain → flour → footprints in the flour → silver coin, both coins → entry fee. The castle turns on a blood curse — `look at two guards` in the dungeon four acts earlier yields the copper key for the casket holding your father's bloodied gold ring; oil it, then wash it in the trough the lame horse is hiding (`pull nail` with the pliers, `move horse`) or the Sheriff's bedchamber crushes your chest and you never get the brass key. Footguns: five hidechildren containers each need a `look at` first (trough, stall, recess, stand, table); `punch sheriff` is ambiguous with "Sheriff's bedchamber" and leaves a disambiguation menu that eats the next command, so it is spelled `punch sheriff of nottingham`; the cloak must be worn *before* paying the entry fee; and the arrow economy is exact — 5 arrows, 4 needed (rabbit, two contest rounds, the chandelier rope that fells Gisbourne). errors=0, no RNG; native `aslx_replay` byte-identical. |
| Beowulf | Finished | short linear retelling of the Grendel fight (anonymous, Quest 5.0.4303, Dec 2011, ASL 500); no published walkthrough exists — the 11-step winning script was derived from the game source. The game is a corridor of death endings with one win: `tear off arm` while Grendel is in the mead hall at night ("Congratulations on beating the game"). Footguns, all author bugs: the Field→Mead Hall exit refuses you while carrying the sword, but its check only destroys a marker object on the first try and then refuses *forever*, so `drop sword` must precede the first `north` or the hall is permanently unreachable; `sail ship` must name the ship, since the bare lowercase `<sail>` script is dead code referencing a nonexistent capital `Ship`; and answering "no" to Hrothgar's "Do you still go on?" is an instant GAME OVER. In the Breca flashback only `use sword on sea monster` survives (swimming toward it or bare hands are fatal), and its "Do you hold on?" is answered **no** deliberately — every branch returns to the mead hall, but the yes-branch rolls `RandomChance(75)` while the no-branch is deterministic *and* the better outcome (you stay in the swimming race instead of losing it). **This game is why the native engine learned the expression form of `Ask`** (`Interp::ask_provider`, alongside the existing `menu_provider`): it uses `if (Ask("…"))`, which QuestViva awaits mid-expression, and the engine previously had only the `ask` statement — it reported `Unknown function 'Ask'` and the win was unreachable natively. errors=0; native `aslx_replay` byte-identical. |
| Fountain of Eternal Youth v1.3 | Finished (errors=1) | small self-contained gem hunt in a garden maze (Quest 5.8.6836.13983, May 2019, ASL 580); no published walkthrough exists — the 29-step winning script was derived from the game source. Part two was announced and never released, so the "you have completed part one" ending IS the whole game. Win = ruby/sapphire/amber onto the colour-matched red/blue/yellow pedestals, whose turnscript dissolves the steel door and `MoveObject`s you into `End`, which `finish`es. The lantern must be **carried and lit** before the first `north`, or the Sandstone Passageway's `enter` drops you down a concealed chute; the six tunnels below all alias to "Worm Tunnel" so the banner never says which one you are in, and Worm Tunnel5 (west of WT1) is an instant `finish` — the purple worm eats you. Chain: shovel (WT4) → `dig dirt with shovel` in **WT6 specifically** (all four synonymous dig scripts gate on `game.pov.parent = Worm Tunnel6`) unlocks the avalanche exit up to the West Garden; sword (WT3) is required to `cut roots with sword` (a scriptdictionary keyed on the sword; the roots have no other harvest). Cauldron riddle "what the small folk eat, and the big folk's mouths" = fungi + roots, any one of each → sapphire; helmet → the father statue → ruby; a plum → the starving-man statue (name `statue2`, alias `statue`) → amber. `take plums` raises a four-way disambiguation between four identical clones, answered with a bare `1` — leaving it unanswered silently swallows the take. errors=1 is the author's own: the red pedestal's `_initialise_` is a bare `LockExit` with no argument. **This game is why the native engine learned Quest's pre-call arity check** (`No parameters passed to X function - expected N parameters`, ASL 520+); it previously ran the body and reported `Unknown object or variable 'exit'`. Native `aslx_replay` byte-identical. |
