# ProbeDelrt -- <del> conformance, measured against the real ADRIFT 5.0.36
# Runner (run500.exe under Wine) rather than against FrankenDrift, which did
# not implement <del> at all.  See test/adrift5/probes/src/delrt.xml.
#
# Cases 1-7 render at load (Introduction + room description); `eight` adds the
# cross-message case (a <del> reaching back across a message boundary).
# `nine`/`ten`/`eleven` pin down pSpace's raw-tail test: the runner adds the
# two join spaces whenever the previous message's RAW text ends in a tag's '>'
# (nine: trailing <del> with a surviving newline; ten: trailing <br>), and
# suppresses them only for a bare source newline (eleven).
eight
nine
ten
eleven
