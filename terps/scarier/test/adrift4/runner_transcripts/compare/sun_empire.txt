feed        84 commands
runner      82 turns echoed
scarier     82 turns, feed[0] is scarier turn 0

RULE 2 -- 2 command(s) the Runner never echoed.  Everything
after the first of them is out of step and is NOT an engine
difference until the feed is fixed and the row re-driven:
    feed[82]  quit
    feed[83]  y

turn 81  w -- the Runner appended [Press any key to end], which is the host's own keypress prompt (ALR-rewritten in some games), not game text.  The turn is identical either side of it.

1 turn(s) differed only by the Runner's trailing keypress prompt.

(the openings differ too -- banner, graphics notice or the
 Runner's own startup lines; usually not an engine difference)
