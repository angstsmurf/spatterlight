# ProbeWaitkey -- synthetic conformance probe from the adrift-5-rs test suite
# (test/a5probes/src/waitkey.xml, section `waitkey_walkthrough`).
# Exercises <waitkey> intro pauses (keypresses are no-ops headless).
look
